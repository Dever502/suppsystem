services:
  postgres:
    image: postgres:16.14-alpine3.22@sha256:786dab398303b8ce7cb76b407bb21ef2e4dfbbbd4c6abcf3d29b3130467ffdbc
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=64m,mode=1777,nosuid,nodev,noexec
    stop_grace_period: 60s
    ports:
      - "${POSTGRES_PUBLISH_HOST:-127.0.0.1}:${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-suppsystem}
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD:?POSTGRES_ADMIN_PASSWORD must be set}
      POSTGRES_MIGRATION_USER: suppsystem_migrator
      POSTGRES_MIGRATION_PASSWORD: ${POSTGRES_MIGRATION_PASSWORD:?POSTGRES_MIGRATION_PASSWORD must be set}
      POSTGRES_RUNTIME_USER: suppsystem_runtime
      POSTGRES_RUNTIME_PASSWORD: ${POSTGRES_RUNTIME_PASSWORD:?POSTGRES_RUNTIME_PASSWORD must be set}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  postgres-provision:
    image: "${APP_IMAGE:?APP_IMAGE must be set to an immutable registry image}"
    restart: "no"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=16m,mode=1777,nosuid,nodev,noexec
    depends_on:
      postgres:
        condition: service_healthy
    command: ["python", "-m", "suppsystem.postgres_provision"]
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_DB: ${POSTGRES_DB:-suppsystem}
      POSTGRES_ADMIN_USER: postgres
      POSTGRES_ADMIN_PASSWORD: ${POSTGRES_ADMIN_PASSWORD:?POSTGRES_ADMIN_PASSWORD must be set}
      POSTGRES_MIGRATION_USER: suppsystem_migrator
      POSTGRES_MIGRATION_PASSWORD: ${POSTGRES_MIGRATION_PASSWORD:?POSTGRES_MIGRATION_PASSWORD must be set}
      POSTGRES_RUNTIME_USER: suppsystem_runtime
      POSTGRES_RUNTIME_PASSWORD: ${POSTGRES_RUNTIME_PASSWORD:?POSTGRES_RUNTIME_PASSWORD must be set}

  postgres-migrate:
    image: "${APP_IMAGE:?APP_IMAGE must be set to an immutable registry image}"
    restart: "no"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=16m,mode=1777,nosuid,nodev,noexec
    depends_on:
      postgres-provision:
        condition: service_completed_successfully
    command: ["python", "-m", "suppsystem.migrations"]
    environment:
      MIGRATION_DATABASE_URL: postgresql+asyncpg://suppsystem_migrator:${POSTGRES_MIGRATION_PASSWORD:?POSTGRES_MIGRATION_PASSWORD must be set}@postgres:5432/${POSTGRES_DB:-suppsystem}
      POSTGRES_ADMIN_PASSWORD: ""
      POSTGRES_MIGRATION_PASSWORD: ""
      POSTGRES_RUNTIME_PASSWORD: ""

  suppsystem:
    image: "${APP_IMAGE:?APP_IMAGE must be set to an immutable registry image}"
    env_file: "${SUPPSYSTEM_ENV_FILE:-/opt/suppsystem/.env}"
    ports:
      - "${API_PUBLISH_HOST:-127.0.0.1}:${API_PORT:-8080}:${API_PORT:-8080}"
    restart: unless-stopped
    init: true
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    tmpfs:
      - /tmp:size=64m,mode=1777,nosuid,nodev,noexec
    stop_grace_period: 90s
    volumes:
      - suppsystem_data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      postgres-provision:
        condition: service_completed_successfully
      postgres-migrate:
        condition: service_completed_successfully
    environment:
      DATABASE_URL: postgresql+asyncpg://suppsystem_runtime:${POSTGRES_RUNTIME_PASSWORD:?POSTGRES_RUNTIME_PASSWORD must be set}@postgres:5432/${POSTGRES_DB:-suppsystem}
      MIGRATION_DATABASE_URL: ""
      MIGRATIONS_AT_STARTUP: "false"
      POSTGRES_ADMIN_PASSWORD: ""
      POSTGRES_MIGRATION_PASSWORD: ""
      POSTGRES_RUNTIME_PASSWORD: ""

    healthcheck:
      test: ["CMD", "python", "-m", "suppsystem.healthcheck"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

volumes:
  postgres_data:
    name: "${POSTGRES_VOLUME_NAME:-suppsystem_postgres_data}"

  suppsystem_data:
    name: suppsystem_data
